import 'package:flutter/material.dart';

class SignUserIn extends StatelessWidget {
  const SignUserIn({super.key});


  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey[850],
    );
  }
}